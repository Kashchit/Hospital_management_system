<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Hospital Management System</title>
    <style>
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            font-family: Arial, sans-serif;
            background-color: #f7f7f7;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
        }

        header {
            background-color: #4CAF50;
            color: white;
            padding: 20px 0;
        }

        header h1 {
            margin: 0;
        }

        nav ul {
            list-style-type: none;
            margin: 0;
            padding: 0;
        }

        nav ul li {
            display: inline;
            margin-right: 20px;
        }

        nav ul li a {
            color: white;
            text-decoration: none;
            transition: color 0.3s;
        }

        nav ul li a:hover {
            color: #fff;
        }

        .hero {
            background-color: #fff;
            padding: 100px 0;
            text-align: center;
        }

        .hero h2 {
            margin-bottom: 20px;
        }

        .footer {
            background-color: #4CAF50;
            color: white;
            text-align: center;
            padding: 20px 0;
        }
        li:hover{
            text-decoration: underline;
        }

        img{
            width: 550px;
            height: auto;
        }
        .homebtn {
        color: #f7f7f7;
        background-color: #4CAF50;
        text-decoration: none;
        padding:15px 30px;
        border-radius: 5px;
        border: none;
        cursor: pointer;
        margin-top: 30px;
        margin-left: 20px;
        transition: background-color 0.3s, color 0.3s;
        }

        .homebtn:hover {
        background-color: #45a049;
        color: #fff;
        transform: translateY(3px);
        }

        
    </style>
</head>
<body>
    <!-- <header>
        <div class="container">
            <h1>Hospital Management System</h1>
            <br>
            <nav>
                <ul>
                    
                    <% if (user) { %>
                        <li><a href="/auth/logout">Logout</a></li>
                    <% } else { %>
                        <li><a href="/auth/login">Login</a></li>
                        <li><a href="/auth/register">Sign Up</a></li>
                    <% } %>
                </ul>
            </nav>
        </div>
    </header> -->
    <section class="hero">
        <div class="container">
            <img src="/img/img2.png">
            <h2>Welcome to our Hospital Management System</h2>
            <p>Efficiently manage patients, doctors, and appointments.</p>
            
                <% if (user) { %>
                    <li><a href="/auth/logout">Logout</a></li>
                <% } else { %>
                    <a href="/auth/login"><button class="homebtn">Login</button></a>
                   <a href="/auth/register"> <button class="homebtn">Sign Up</button></a>
                <% } %>
           
        </div>
    </section>
    <footer class="footer">
        <div class="container">
            <p>Hospital Management System © 2024</p>    
        </div>
    </footer>
</body>
</html>
